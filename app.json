{
    "name": "grand-stack-starter",
    "env": {
        "GRAPHENEDB_BOLT_URL": {
            "description": "The bolt endpoint for your neo4j database. For example: bolt://grandstack.io:7687",
            "required": true
        },
        "GRAPHENEDB_BOLT_USER": {
            "description": "The database user that the API application will use to connect to Neo4j",
            "required": true
        },
        "GRAPHENEDB_BOLT_PASSWORD": {
            "description": "The password for the provided Neo4j database user",
            "required": true
        }
    }
}